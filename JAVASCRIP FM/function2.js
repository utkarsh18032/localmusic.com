/*Que.1 write a javascript function which returns "you are eligible for driving !"
if  the condition is true otherwise it will return 
" you are not elegible for driving !"*/

function driving(age)
{
    if(age>18)
    console.log("you are eligible for driving")

    else
    console.log("you are not eligible for driving")
}

driving(10);